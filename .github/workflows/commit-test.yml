name: Commit test
on: [push]
jobs:
  prepare_release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - run: |
          ls
          composer install --no-dev
          ls
          rm codeception.dist.yml && rm readme.md && rm -rf tests && rm composer.json && rm composer.lock
          ls
